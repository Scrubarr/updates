# Scrubarr Updates

Public update manifests for Scrubarr.

- Stable channel: `stable.json`

Scrubarr reads this manifest to detect when a newer Docker image is available.
The app does not auto-install updates from this file.
